package org.example.kunDSA.OOPSfeature;

public class Main {
    public static void main(String[] args) {
        //Box box = new Box();  //only  parent because constructor of BOX
        //Box box = new BoxWeight(2);  //access only parent part due to reference variable
        //BoxWeight boxWeight = new BoxWeight(30); //can access every part both constructor work
        //BoxWeight boxWeight = new Box(); // errror due to no constructor of like boxweight class    }
    }
}
